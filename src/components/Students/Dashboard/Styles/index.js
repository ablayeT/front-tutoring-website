import {
  Search,
  SearchIconWrapper,
  StyledInputBase,
  DrawerHeader,
  AppBar,
} from './Dashboard.style';
export { Search, SearchIconWrapper, StyledInputBase, DrawerHeader, AppBar };

import { useStyles } from './Dashboard.style';
export default useStyles;
